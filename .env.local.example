# Supabase (safe to expose client-side: RLS is the real gate, not this key)
NEXT_PUBLIC_SUPABASE_URL=https://okkfgztwvondfdbvxasu.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Cloudflare Worker (unifind-worker) base URL
# Local:  http://127.0.0.1:8787
# Prod:   https://unifind-worker.<your-subdomain>.workers.dev  (or your bound custom domain)
NEXT_PUBLIC_WORKER_URL=
